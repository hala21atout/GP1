name: gp1
description: "A new Flutter project."

# Optional: Remove this line if you want to publish to pub.dev
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=3.5.4 <4.0.0"  # Set a range to ensure compatibility.

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8  # This is fine as is.
  font_awesome_flutter: ^10.1.0  # Keep only this for FontAwesome icons

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0  # Keep this updated.

flutter:
  assets:
    - assets/logo1.png  # Make sure this path is correct.
    - assets/makeup.jpg
    - assets/skincare.jpg
    - assets/doctors.jpg
  uses-material-design: true
